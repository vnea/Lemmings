ATTENTION : la case (0,0) est celle en haut a gauche.
ATTENTION : Dans l'implem ne passe pas un service a un autre en param du constructeur. ça se fait avec la méthode bind des itf require. 
		    Surement virer les getters pour les services aussi, ils sont juste dans la spec pour expliciter l'utilisation.

//Qui s'occupe de l'affichage ? les lemmings gerent eux memes leur affichage ?
//notion d'enchainement des evenements dans le temps ?

- est ce qu'on peut gerer proprement des pre ou est ce qu'elle doivent vraiment jamais arrive ? sinon retirer pre de useToken
- voir si des observateurs sont private en réalité 

//liaison entre les services !?  ON LES PASSENT EN PARAM
//	- observator gE ? possibilité d'indiquer le passage de gameEng a Lemming en post de newLemming
//			pareil pour lvl et gameEng. A ce compte là il faut les passer au constructeur. Mais c'est plus du require/provide

dans Level :
//	- voir si on garde invariant pour squareExist ? Sinon le remettre dans les post de init
//	- voir convergence de setNature (mais je ne vois pas comment faire autrement)
	- opérateur pour repasser en mode edition ?
	- voir qu'on ne puisse pas définir l'entrée et la sortie une fois en mode play (surement un truc propre à l'implem)
	
dans GameEng : 
	- les observateurs et operators sont-ils utilisables après gameOver 
//	- score pourrait etre invariant mais n'est accessible qu'une vois la partie terminée
	
//	- obstacle invariant par rapport a nature qui est dans un autre service ? (Si non alors surement mettre squareExist en pré)
	
	- ? mettre en invariant : nbLemmingsCreated(G) = (tour(G)+spawnSpeed(G)-1) / spawnSpeed(G)  //('/' is the whole division) //minisation  
		pour mettre ce genre de truc il faut qu'un tour soit atomique. donc pas de newLemming, et ptet pas de saveLemming et killLemming. 
		il y aurait juste un executeTour
	- post de callStepLs
	
	
dans Lemming :
//			=> doit-on mettre dans les invariants des bornes aux valeur de h et w. normalement pas possible car toutes les cases autour sont metal
//			ça voudrait dire que Lemming appel observators de Level
            - voir refine

	
dans Player:
	 
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	